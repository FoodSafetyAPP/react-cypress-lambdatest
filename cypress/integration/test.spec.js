/// <reference types="cypress" />

describe('visit cypress', () => {
  it('visit url', () => {
    cy.visit('http://localhost:3000');
  });
});

describe('visit cypress test', () => {
  it('visit url', () => {
    cy.visit('http://localhost:3000');
  });
});
