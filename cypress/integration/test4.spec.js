/// <reference types="cypress" />

describe('visit cypress', () => {
  it('visit url', () => {
    cy.visit('http://localhost:3000');
  });
});

describe('visit cypress1', () => {
  it('visit url1', () => {
    cy.visit('http://localhost:3000');
  });
});
