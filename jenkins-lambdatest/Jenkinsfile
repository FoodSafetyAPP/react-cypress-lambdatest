pipeline {
  agent any

  environment {
    LT_USERNAME = credentials('LT_USERNAME')
    LT_ACCESS_KEY = credentials('LT_ACCESS_KEY')
  }

  stages {

    stage('execute exit') {
      steps {
       echo "${currentBuild.result}";
      }
    }
  }
}