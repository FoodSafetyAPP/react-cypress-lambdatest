pipeline {
  agent {
    docker {
      image 'cypress/base:16.14.0'
      args '-e HOME=/tmp -e NPM_CONFIG_PREFIX=/tmp/.npm'
    }
  }

  stages {

    stage('verify cypress') {
      steps {
        sh '$(npm bin)/cypress info'
      }
    }
}
